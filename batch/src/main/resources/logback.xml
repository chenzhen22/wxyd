<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="60 seconds" debug="false">

    <!--彩色日志-->
    <conversionRule conversionWord="clr" converterClass="org.springframework.boot.logging.logback.ColorConverter"/>
    <conversionRule conversionWord="wex"
                    converterClass="org.springframework.boot.logging.logback.WhitespaceThrowableProxyConverter"/>
    <conversionRule conversionWord="wEx"
                    converterClass="org.springframework.boot.logging.logback.ExtendedWhitespaceThrowableProxyConverter"/>

    <property name="LOG_PATH" value="/logs/app_logs/wxyd/"/>
    <property name="CONSOLE_LOG_PATTERN"
              value="${CONSOLE_LOG_PATTERN:-%clr(%d{${LOG_DATEFORMAT_PATTERN:-yyyy-MM-dd HH:mm:ss.SSS}}){faint} %clr(${LOG_LEVEL_PATTERN:-%5p}) %clr(${PID:- }){magenta} %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}}"/>
    <property name="FILE_LOG_PATTERN"
              value="${FILE_LOG_PATTERN:-%d{${LOG_DATEFORMAT_PATTERN:-yyyy-MM-dd HH:mm:ss.SSS}} ${LOG_LEVEL_PATTERN:-%5p} ${PID:- } --- [%t] %-40.40logger{39} : %replace(%msg){'^(.{5000}).*$', '$1'}%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}}"/>
    <!--最大保存天数-->
    <property name="KEEP_MAX_DAY" value="7" />
    <!--总最大保存文件大小-->
    <property name="KEEP_TOTAL_MAX_SIZE" value="1GB" />
    <!--单文件最大保存文件大小-->
    <property name="KEEP_FILE_MAX_SIZE" value="100MB" />
    <!-- 跟踪日志-->
    <appender name="TRACE" class="com.chenzhen.appender.TraceLogConsoleAppender"/>

    <!-- 控制台日志 -->
    <appender name="CONSOLE" class="com.chenzhen.logback.WxydConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
            <immediateFlush>false</immediateFlush>
        </encoder>
    </appender>

    <!-- 业务日志 -->
    <appender name="FILE_ALL" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/batch/batch.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/history/batch/%d{yyyy-MM}/batch.log.%d{yyyy-MM-dd}.%i.gz</fileNamePattern>
            <maxFileSize>${KEEP_FILE_MAX_SIZE}</maxFileSize>
            <maxHistory>${KEEP_MAX_DAY}</maxHistory>
            <totalSizeCap>${KEEP_TOTAL_MAX_SIZE}</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>${FILE_LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 错误日志 -->
    <appender name="FILE_ERROR" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <encoder>
            <pattern>${FILE_LOG_PATTERN}</pattern>
            <immediateFlush>false</immediateFlush>
        </encoder>
        <file>${LOG_PATH}/batch/error.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/history/batch/%d{yyyy-MM}/error.log.%d{yyyy-MM-dd}.%i.gz</fileNamePattern>
            <maxFileSize>${KEEP_FILE_MAX_SIZE}</maxFileSize>
            <maxHistory>${KEEP_MAX_DAY}</maxHistory>
            <totalSizeCap>${KEEP_TOTAL_MAX_SIZE}</totalSizeCap>
        </rollingPolicy>
        <!-- 过滤出ERROR级别的日志 -->
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
    </appender>

    <appender name="ASYNC-CONSOLE" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="CONSOLE"/>
        <neverBlock>true</neverBlock>
    </appender>

    <appender name="ASYNC-FILE_ALL" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="FILE_ALL"/>
        <neverBlock>true</neverBlock>
    </appender>

    <appender name="ASYNC-FILE_ERROR" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="FILE_ERROR"/>
        <neverBlock>true</neverBlock>
    </appender>

    <!-- 日志级别 -->
    <root level="INFO">
        <appender-ref ref="ASYNC-CONSOLE"/>
        <appender-ref ref="ASYNC-FILE_ALL"/>
        <appender-ref ref="ASYNC-FILE_ERROR"/>
        <!--        <appender-ref ref="TRACE"/>-->
    </root>

    <!-- 日志过滤 -->
    <logger name="org.apache.catalina.startup.DigesterFactory" level="ERROR"/>
    <logger name="org.apache.tomcat.util.net.NioSelectorPool" level="WARN"/>
    <logger name="org.springframework.boot.web.embedded.tomcat" level="OFF"/>
    <logger name="com.alibaba.nacos.client" level="ERROR"/>
    <logger name="com.chenzhen.mapper" level="info"/>
</configuration>